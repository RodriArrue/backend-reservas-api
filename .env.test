# Variables de ambiente para tests
NODE_ENV=test

# Base de datos de test (usar una diferente a producción)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=reservas_test
DB_USER=postgres
DB_PASSWORD=postgres
DB_DIALECT=postgres

# JWT para tests
JWT_SECRET=test_jwt_secret_for_testing_only
REFRESH_TOKEN_SECRET=test_refresh_secret_for_testing_only

# CSRF para tests
CSRF_SECRET=test_csrf_token
